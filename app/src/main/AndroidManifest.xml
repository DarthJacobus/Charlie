<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.migrainebuddy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".LogIn_CreateAccount.AgeActivity"></activity>
        <activity android:name=".MaleQuestionsActivity" />
        <activity android:name=".LogIn_CreateAccount.GenderSpecificQuestions" />
        <activity android:name=".LogIn_CreateAccount.GenderActivity" />
        <activity android:name=".Profile_and_Settings.EditProfileActivity" />
        <activity android:name=".Profile_and_Settings.SettingsActivity" />
        <activity android:name=".BottomNavigation.BottomNavigationActivity" />
        <activity android:name=".LogIn_CreateAccount.PasswordActivity" />
        <activity android:name=".LogIn_CreateAccount.EmailActivity" />
        <activity android:name=".LogIn_CreateAccount.NameActivity" />
        <activity android:name=".LogIn_CreateAccount.SignInActivity" />
        <activity android:name=".LogIn_CreateAccount.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>